# lftp action

This action uses lftp to sync a directory to an external server.

## Inputs

**Required** USERNAME
**Required** PASSWORD
**Required** HOST

## Example usage

uses: actions/lftp@v1
with:
    USERNAME: Mister
    PASSWORD: Welcome!
    HOST: myserver.com
